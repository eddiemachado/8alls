{
  "name": "8alls",
  "version": "1.0.0",
  "private": true,
  "description": "8alls - Personal productivity suite",
  "workspaces": [
    "packages/*",
    "mcp-servers/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^1.11.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
